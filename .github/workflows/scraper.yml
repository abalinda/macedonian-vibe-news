name: Vibes Scraper

on:
  schedule:
  # - cron: '0 */12  * * *'      # 00:00, 12:00
  # - cron: '30 1-23/3 * * *'  # 01:30, 04:30, 07:30, ..
  workflow_dispatch: # Allows manual run button

# Prevent multiple scrapers from running at once
concurrency:
  group: scraper-prod
  cancel-in-progress: true

jobs:
  scrape-news:
    name: Scrape & AI Curate
    runs-on: ubuntu-latest
    timeout-minutes: 15  # 🛑 Safety Net: Kills job if it hangs > 15 mins

    steps:
      - name: 📥 Check out code
        uses: actions/checkout@v4

      - name: 🐍 Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip' # Auto-caches pip packages based on requirements.txt

      - name: 📦 Install dependencies
        working-directory: ./scraper
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🤖 Run Scraper
        working-directory: ./scraper
        env:
          # Inject secrets from GitHub
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python scraper_2.py
